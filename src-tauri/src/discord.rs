// use tauri::{State, App, Manager};
// use tokio::time::{interval, Duration};

// use crate::tauri::{DiscordRP, PrefsStore};

// pub fn discord_ipc_start_interval(client: State<'_, DiscordRP>) {


//     // tokio::async_runtime::spawn(async move {

//     //     let mut interval = interval(Duration::from_secs(1));

//     //     loop {
//     //         interval.tick().await;
//     //         println!("{:?}", client);

//             // let prefs = prefs.0.lock().unwrap();

//             // println!("{&client:?}");
//     //     }

//     // });
  
// }